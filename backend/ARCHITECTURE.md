# FlashyBank - Arquitectura del Sistema

## 🏗️ Diagrama de Arquitectura - JWT Authentication

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              CLIENTE (React Native App)                         │
│                                                                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────────────────────┐  │
│  │ Login Screen │  │  Home Screen │  │       Transfer Screen                │  │
│  │              │  │              │  │  ┌─────────┐  ┌──────────────────┐   │  │
│  │ Username:    │  │  Balance:    │  │  │ To:     │  │  Biometric Auth  │   │  │
│  │ [juan     ]  │  │  $1,000.00   │  │  │ [maria ]│  │  ┌──────────────┐ │   │  │
│  │ Password:    │  │              │  │  │         │  │  │ TouchID/     │ │   │  │
│  │ [********]   │  │  ┌────────┐  │  │  │ Amount: │  │  │ FaceID       │ │   │  │
│  │              │  │  │Send $  │  │  │  │ [$100  ]│  │  └──────────────┘ │   │  │
│  │  [LOGIN]     │  │  └────────┘  │  │  │         │  │                   │   │  │
│  └──────────────┘  │              │  │  │ [SEND]  │  │  [CONFIRM]       │   │  │
│                    └──────────────┘  │  └─────────┘  └──────────────────┘   │  │
│                                      └──────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────────────┘
                                          │
                                          │ HTTP/HTTPS
                                          │ JSON
                                          ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│                         FLASHYBANK API (Spring Boot)                            │
│                                                                                 │
│  ┌──────────────────────────────────────────────────────────────────────────┐  │
│  │                     Spring Security Filter Chain                         │  │
│  │                                                                          │  │
│  │  ┌────────────────────────────────────────────────────────────────────┐ │  │
│  │  │  JwtAuthenticationFilter                                           │ │  │
│  │  │  ┌──────────────────────────────────────────────────────────────┐  │ │  │
│  │  │  │ 1. Extract token from "Authorization: Bearer <token>"       │  │ │  │
│  │  │  │ 2. Validate token signature with JWT secret                  │  │ │  │
│  │  │  │ 3. Check token expiration                                    │  │ │  │
│  │  │  │ 4. Extract username from token                               │  │ │  │
│  │  │  │ 5. Load UserDetails from database                            │  │ │  │
│  │  │  │ 6. Set Authentication in SecurityContext                     │  │ │  │
│  │  │  └──────────────────────────────────────────────────────────────┘  │ │  │
│  │  └────────────────────────────────────────────────────────────────────┘ │  │
│  │                              │                                          │  │
│  │                              ▼                                          │  │
│  │  ┌────────────────────────────────────────────────────────────────────┐ │  │
│  │  │  SecurityConfig                                                    │ │  │
│  │  │  ┌──────────────────────────────────────────────────────────────┐ │ │  │
│  │  │  │ Public: /api/auth/**, /api/public/**                         │ │ │  │
│  │  │  │ Protected: /api/** (requires valid JWT)                      │ │ │  │
│  │  │  │ Session: STATELESS (no HTTP sessions)                        │ │ │  │
│  │  │  └──────────────────────────────────────────────────────────────┘ │ │  │
│  │  └────────────────────────────────────────────────────────────────────┘ │  │
│  └──────────────────────────────────────────────────────────────────────────┘  │
│                                   │                                             │
│                                   ▼                                             │
│  ┌──────────────────────────────────────────────────────────────────────────┐  │
│  │                            Controllers                                   │  │
│  │                                                                          │  │
│  │  ┌──────────────────────────┐  ┌──────────────────────────────────────┐ │  │
│  │  │   AuthController         │  │     TransactionController            │ │  │
│  │  │                          │  │                                      │ │  │
│  │  │ POST /auth/register      │  │  POST /transactions/initiate        │ │  │
│  │  │ POST /auth/login         │  │  POST /transactions/confirm/{id}    │ │  │
│  │  │ POST /auth/refresh       │  │  GET  /transactions/history         │ │  │
│  │  └──────────────────────────┘  └──────────────────────────────────────┘ │  │
│  └──────────────────────────────────────────────────────────────────────────┘  │
│                                   │                                             │
│                                   ▼                                             │
│  ┌──────────────────────────────────────────────────────────────────────────┐  │
│  │                             Services                                      │  │
│  │                                                                          │  │
│  │  ┌──────────────────────────┐  ┌──────────────────────────────────────┐ │  │
│  │  │   AuthService            │  │   TransactionService (PRÓXIMO)       │ │  │
│  │  │                          │  │                                      │ │  │
│  │  │ - login()                │  │  - initiateTransfer()                │ │  │
│  │  │ - register()             │  │  - confirmTransfer()                 │ │  │
│  │  │ - refreshToken()         │  │  - getHistory()                      │ │  │
│  │  └──────────────────────────┘  └──────────────────────────────────────┘ │  │
│  │                                                                          │  │
│  │  ┌────────────────────────────────────────────────────────────────────┐ │  │
│  │  │         UserDetailsServiceImpl                                     │ │  │
│  │  │         - loadUserByUsername()                                     │ │  │
│  │  └────────────────────────────────────────────────────────────────────┘ │  │
│  └──────────────────────────────────────────────────────────────────────────┘  │
│                                   │                                             │
│                                   ▼                                             │
│  ┌──────────────────────────────────────────────────────────────────────────┐  │
│  │                           JWT Util                                       │  │
│  │                                                                          │  │
│  │  ┌────────────────────────────────────────────────────────────────────┐ │  │
│  │  │  JwtUtil                                                           │ │  │
│  │  │  ┌──────────────────────────────────────────────────────────────┐ │ │  │
│  │  │  │ - generateToken(UserDetails) → String                        │ │ │  │
│  │  │  │ - generateRefreshToken(UserDetails) → String                 │ │ │  │
│  │  │  │ - extractUsername(String) → String                           │ │ │  │
│  │  │  │ - extractExpiration(String) → Date                           │ │ │  │
│  │  │  │ - isTokenValid(String, UserDetails) → boolean                │ │ │  │
│  │  │  │ - isTokenExpired(String) → boolean                           │ │ │  │
│  │  │  └──────────────────────────────────────────────────────────────┘ │ │  │
│  │  └────────────────────────────────────────────────────────────────────┘ │  │
│  └──────────────────────────────────────────────────────────────────────────┘  │
│                                   │                                             │
└───────────────────────────────────┼─────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│                        PostgreSQL Database                                      │
│                                                                                 │
│  ┌──────────────────────────────┐  ┌─────────────────────────────────────────┐ │
│  │        users                 │  │      transactions                       │ │
│  │                              │  │                                         │ │
│  │  ┌────────────────────────┐  │  │  ┌────────────────────────────────────┐│ │
│  │  │ id          BIGINT     │  │  │  │ id           BIGINT                ││ │
│  │  │ username    VARCHAR    │  │  │  │ sender_id    BIGINT                ││ │
│  │  │ password    VARCHAR    │  │  │  │ receiver_username VARCHAR         ││ │
│  │  │ balance     DECIMAL    │  │  │  │ amount       DECIMAL               ││ │
│  │  │ role        VARCHAR    │  │  │  │ status       VARCHAR               ││ │
│  │  │ enabled     BOOLEAN    │  │  │  │ description  VARCHAR               ││ │
│  │  │ created_at  TIMESTAMP  │  │  │  │ created_at   TIMESTAMP             ││ │
│  │  │ updated_at  TIMESTAMP  │  │  │  └────────────────────────────────────┘│ │
│  │  └────────────────────────┘  │  └─────────────────────────────────────────┘ │
│  └──────────────────────────────┘                                              │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Flujo de Autenticación Detallado

### 1. Registro de Usuario
```
Client                              Spring Boot API                    PostgreSQL
  │                                    │                                   │
  │ POST /api/auth/register            │                                   │
  │ {username, password} ─────────────>│                                   │
  │                                    │                                   │
  │                                    │ UserRepository.existsByUsername()│
  │                                    │ ────────────────────────────────>│
  │                                    │ <────────────────────────────────│
  │                                    │ false                             │
  │                                    │                                   │
  │                                    │ passwordEncoder.encode(password) │
  │                                    │ BCrypt ────────────────────────> │
  │                                    │ ────────────────────────────────>│
  │                                    │ user.setPassword(hash)           │
  │                                    │                                   │
  │                                    │ userRepository.save(user) ──────>│
  │                                    │ INSERT INTO users...             │
  │                                    │ <────────────────────────────────│
  │                                    │ user (con id generado)           │
  │                                    │                                   │
  │                                    │ jwtUtil.generateToken()           │
  │                                    │ ────────────────────────────────> │
  │                                    │ JWT (access + refresh)            │
  │                                    │                                   │
  │<───────────────────────────────────│ LoginResponse                      │
  │ {accessToken, refreshToken, ...}   │                                   │
```

### 2. Login
```
Client                              Spring Boot API                    PostgreSQL
  │                                    │                                   │
  │ POST /api/auth/login               │                                   │
  │ {username, password} ─────────────>│                                   │
  │                                    │                                   │
  │                                    │ authenticationManager.authenticate()
  │                                    │ UserDetailsServiceImpl.loadUserByUsername()
  │                                    │ ────────────────────────────────>│
  │                                    │ SELECT * FROM users WHERE...     │
  │                                    │ <────────────────────────────────│
  │                                    │ User entity                       │
  │                                    │                                   │
  │                                    │ BCrypt check password              │
  │                                    │                                   │
  │                                    │ jwtUtil.generateToken(userDetails)│
  │                                    │ jwtUtil.generateRefreshToken()    │
  │                                    │                                   │
  │<───────────────────────────────────│ LoginResponse                      │
  │ {accessToken, refreshToken, ...}   │                                   │
```

### 3. Acceso a Endpoint Protegido
```
Client                              Spring Boot API                    PostgreSQL
  │                                    │                                   │
  │ GET /api/protected                 │                                   │
  │ Authorization: Bearer <token> ────>│                                   │
  │                                    │                                   │
  │                                    │ JwtAuthenticationFilter           │
  │                                    │ ├── Extract token from header     │
  │                                    │ ├── jwtUtil.extractUsername()     │
  │                                    │ ├── UserDetailsService.loadUserByUsername()
  │                                    │ │ ──────────────────────────────>│
  │                                    │ │ SELECT * FROM users...          │
  │                                    │ │ <─────────────────────────────│
  │                                    │ │ UserDetails                     │
  │                                    │ ├── jwtUtil.isTokenValid()        │
  │                                    │ │ ├── Verify signature            │
  │                                    │ │ ├── Check expiration            │
  │                                    │ └── SecurityContext.setAuth()    │
  │                                    │                                   │
  │                                    │ SecurityConfig.authorize()        │
  │                                    │ ✓ Token válido, acceso permitido  │
  │                                    │                                   │
  │                                    │ Controller.protectedEndpoint()    │
  │                                    │ Authentication.getName() ────────>│
  │                                    │                                   │
  │<───────────────────────────────────│ Response con datos                │
  │                                    │                                   │
```

---

## 🔐 Estructura de Token JWT

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           JWT STRUCTURE                                     │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  Header                          Payload                        Signature    │
│  ┌─────────────┐                 ┌─────────────────────┐        ┌────────┐  │
│  │             │                 │                     │        │        │  │
│  │ {"alg":    │        .        │ {"sub": "juan",     │   .    │ HMAC-  │  │
│  │  "HS256",  │                 │   "iat": 1739200000,│        │ SHA256 │  │
│  │  "typ":    │                 │   "exp": 1739800000 │        │ sign   │  │
│  │  "JWT"}    │                 │ }                  │        │        │  │
│  │             │                 │                     │        │        │  │
│  └─────────────┘                 └─────────────────────┘        └────────┘  │
│      Base64URL                        Base64URL                    Calculated│
│      Encoded                          Encoded                    from data  │
│                                                                             │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                          Complete JWT Token                                 │
│                                                                             │
│  eyJhbGciOiJIUzI1NiJ9.eyJzdWIiOiJqdWFuIiwiaWF0IjoxNzM5MjAwMDAwfQ.xxx       │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 🎯 Configuración de Seguridad

### SecurityConfig Configuration
```java
┌─────────────────────────────────────────────────────────────────┐
│                      SecurityConfig                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  CSRF                    DISABLED  (API REST no necesita)       │
│  SessionManagement       STATELESS (sin sesiones HTTP)          │
│                                                                │
│  Public Endpoints:                                              │
│    ├─ /api/auth/**     (login, register, refresh)              │
│    ├─ /api/public/**    (endpoints públicos)                   │
│                                                                │
│  Protected Endpoints:                                           │
│    ├─ /api/**         (requiere token válido)                  │
│                                                                │
│  AuthenticationProvider:  DaoAuthenticationProvider             │
│  PasswordEncoder:        BCryptPasswordEncoder                  │
│  Filter:                JwtAuthenticationFilter                 │
│                                                                │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📊 Modelo de Datos

### User Entity
```java
┌─────────────────────────────────────────────────────────────┐
│  User                                                       │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────────┬──────────────┬─────────────────────────┐ │
│  │ Field        │ Type         │ Description             │ │
│  ├──────────────┼──────────────┼─────────────────────────┤ │
│  │ id           │ Long         │ Primary Key (auto)      │ │
│  │ username     │ String       │ Unique                  │ │
│  │ password     │ String       │ BCrypt hash             │ │
│  │ balance      │ BigDecimal   │ Account balance         │ │
│  │ role         │ String       │ USER, ADMIN             │ │
│  │ enabled      │ Boolean      │ Account active          │ │
│  │ createdAt    │ Timestamp    │ Creation date           │ │
│  │ updatedAt    │ Timestamp    │ Last update             │ │
│  └──────────────┴──────────────┴─────────────────────────┘ │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### Transaction Entity
```java
┌─────────────────────────────────────────────────────────────┐
│  Transaction                                                │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────────────┬──────────────┬─────────────────────┐ │
│  │ Field            │ Type         │ Description         │ │
│  ├──────────────────┼──────────────┼─────────────────────┤ │
│  │ id               │ Long         │ Primary Key (auto)  │ │
│  │ senderId         │ Long         │ Sender user id      │ │
│  │ receiverUsername │ String       │ Receiver username   │ │
│  │ amount           │ BigDecimal   │ Transfer amount     │ │
│  │ status           │ String       │ PENDING, COMPLETED  │ │
│  │ description      │ String       │ Optional notes      │ │
│  │ createdAt        │ Timestamp    │ Creation date       │ │
│  └──────────────────┴──────────────┴─────────────────────┘ │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 🎨 Paquetes y Responsabilidades

```
com.flashybank/
│
├── config/
│   └── SecurityConfig.java
│       └── Configura Spring Security con JWT
│       └── Define endpoints públicos/protegidos
│       └── Configura AuthenticationManager
│
├── controller/
│   ├── AuthController.java
│   │   └── Maneja login, register, refresh
│   └── TestController.java
│       └── Endpoints de prueba
│
├── dto/
│   ├── LoginRequest.java
│   ├── LoginResponse.java
│   ├── RefreshTokenRequest.java
│   └── RegisterRequest.java
│       └── Data Transfer Objects para API
│
├── filter/
│   └── JwtAuthenticationFilter.java
│       └── Intercepta requests y valida JWT
│
├── model/
│   ├── User.java
│   │   └── Entidad JPA de usuario
│   └── Transaction.java
│       └── Entidad JPA de transacción
│
├── repository/
│   ├── UserRepository.java
│   │   └── JPA Repository para usuarios
│   └── TransactionRepository.java
│       └── JPA Repository para transacciones
│
├── service/
│   ├── AuthService.java
│   │   └── Lógica de autenticación
│   └── UserDetailsServiceImpl.java
│       └── Carga usuarios desde BD
│
└── util/
    └── JwtUtil.java
        └── Generación y validación de JWT
```

---

## 🔗 Relaciones Entre Componentes

```
AuthController
      │
      ├── uses ─────────> AuthService
      │                         │
      │                         ├── uses ───> UserRepository
      │                         ├── uses ───> JwtUtil
      │                         ├── uses ───> PasswordEncoder
      │                         └── uses ───> AuthenticationManager
      │
JwtAuthenticationFilter
      │
      ├── uses ─────────> JwtUtil
      ├── uses ─────────> UserDetailsServiceImpl
      │                         │
      │                         └── uses ───> UserRepository
      │
SecurityConfig
      │
      ├── uses ─────────> JwtAuthenticationFilter
      ├── uses ─────────> UserDetailsService
      ├── creates ─────> AuthenticationProvider
      └── creates ─────> PasswordEncoder
```

---

## 🚀 Próximos Componentes (Paso 3)

```
TransactionController (PRÓXIMO)
      │
      ├── POST /transactions/initiate
      │    └── Iniciar transferencia (estado: PENDING)
      │
      ├── POST /transactions/confirm/{id}
      │    └── Confirmar con biometría (estado: COMPLETED)
      │
      └── GET /transactions/history
           └── Historial del usuario

TransactionService (PRÓXIMO)
      │
      ├── validateBalance()
      ├── verifyUserExists()
      ├── createPendingTransaction()
      ├── confirmTransaction()
      └── updateBalances()
```
